@{
    Layout = null;
}
<script>
    function btnEditleads_Click(DashboardCode) {
        var scope = angular.element(document.getElementById("widget-leads")).scope();
        scope.$apply(function () {
            scope.EditCRM00200(DashboardCode);
        });
    }
</script>
<!-- widget grid -->
<div ng-controller="leadsController">
    <div class="widget" id="widget-leads">
        <div ng-show="PagesCRM00200.length > 0">
            <div ng-repeat="Page in PagesCRM00200">
                <div ng-include="Page.url">Content from blocks goes here</div>
            </div>
        </div>
        <div class="k-content" ng-show="PagesCRM00200.length == 0">
            <div class="wide bordered-bottom bordered-palegreen">
                <div kendo-toolbar k-options="toolbarOptions">
                </div>
            </div>
            @*<div>
                @(Html.Kendo().Grid<Kaizen.Admin.CRM00200>()
                        .Name("Gridleads")
                        .Columns(columns =>
                        {
                            columns.Template(@<text></text>).ClientTemplate(@"<a href='javascript:void(0)' onclick=""btnEditleads_Click('#:LeadID#');""> #:LeadID# </a>").Width(100).Title("Lead ID");
                        })
                        .HtmlAttributes(new { style = "height:500px;" })
                        .Scrollable(scrollable => scrollable.Virtual(true))
                        .Pageable(pager => pager.Messages(messages => messages.Display("Showing Data Source from {0} to {1}. Total Sources: <strong>{2}</strong>").Empty("Source Information")).Refresh(true).PageSizes(true).ButtonCount(14))
                        .Sortable(sortable => sortable.AllowUnsort(true).SortMode(GridSortMode.SingleColumn))
                        .Navigatable()
                        .Selectable(selectable => selectable.Mode(GridSelectionMode.Single))
                        .Reorderable(reorder => reorder.Columns(true))
                        .Resizable(ss => ss.Columns(true))
                        .ColumnMenu()
                        .Filterable()
                        .DataSource(dataSource => dataSource.Ajax().ServerOperation(true).PageSize(17)
                        .Read(read => read.Action("GetDataListGrid", "leads")
                                                .Type(HttpVerbs.Get).Data("GetKaizenPublicKey")).Model(model => model.Id(p => p.)))
                )
            </div>*@
            <div kendo-window="GridFilterWindow" k-title="'Filteration'" k-modal="true"
                 k-actions="['Pin', 'Refresh', 'Maximize', 'Close']"
                 k-width="600" k-resizable="true" k-visible="false"></div>
        </div>
    </div>
</div>

